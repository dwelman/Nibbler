#pragma once

class SnakeSegment
{
public:

private:
    enum SnakeSegmentType
    {
        HEAD,
        BODY,
        TAIL
    };

    SnakeSegmentType type;

    enum SegmentDirection
    {
        s
    };
};